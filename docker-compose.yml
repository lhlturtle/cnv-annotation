version: '3.8'

services:
  cnv-annotation:
    build: .
    image: cnv-annotation:latest
    ports:
      - "8502:8502"
    volumes:
      - ./app:/app
      - ./data:/app/data
    environment:
      - STREAMLIT_SERVER_PORT=8502
    restart: unless-stopped
